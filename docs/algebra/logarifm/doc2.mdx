---
id: doc2
title: Свойства логарифма
description: свойства логарифмов, логарифм произведение, логарифм частного, основное логарифмическое тождество
pagination_prev: null
slug: /svoistva logarifma
---

import Image from '@theme/IdealImage';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Accordion from '@mui/material/Accordion';
import AccordionDetails from '@mui/material/AccordionDetails';
import AccordionSummary from '@mui/material/AccordionSummary';
import Typography from '@mui/material/Typography';
import ExpandMoreIcon from '@mui/icons-material/ExpandMore';


---


Логарифмы обладают рядом свойств, которые существенно упрощают вычисления. Эти свойства важно знать 📚.



### Основное логарифмическое тождество

Рассмотрим  пример $2^x=5$, где корень равен $x=\log_25$. Поскольку $x-$ это показатель степени,  равенство $2^x=5$ можно переписать как $2^{\log_25}=5$. 
Это выражение известно как **основное логарифмическое тождество**.

:::info Основное логарифмическое тождество

$$
a^{\log_ab}=b
$$

:::


Например, $2^{\log_232}=32$.

<Tabs>
  <TabItem value="Пример 1" label="Пример №1" default>
 Вычислить $64^{0.5\log_212}$.

 <Accordion>
  <AccordionSummary
    expandIcon={<ExpandMoreIcon />}
    aria-controls="panel1a-content"
    id="panel1a-header"
  >
    <Typography>
      <b>Решение</b>
    </Typography>
  </AccordionSummary>
  <AccordionDetails>
    <Typography>
Преобразуем выражение:


     $64^{0.5\log_212} = \left(2^6\right)^{0.5\log_212}=\left(2^{\log_212} \right)^3$.

     Применяя основное логарифмическое тождество:

     $\left(2^{\log_212} \right)^3=12^3.$

Таким образом, $64^{0.5\log_212}=12^3$.




    </Typography>
  </AccordionDetails>
</Accordion>

  </TabItem>
<TabItem value="Пример №2" label="Пример №2">
   Вычислить $6^{1+\log_65}$.
<Accordion>
  <AccordionSummary
    expandIcon={<ExpandMoreIcon />}
    aria-controls="panel1a-content"
    id="panel1a-header"
  >
    <Typography>
      <b>Решение</b>
    </Typography>
  </AccordionSummary>
  <AccordionDetails>
    <Typography>

Применим свойство степени и основное логарифмическое тождество:

   $6^{1+\log_65}=6\cdot 6^{\log_65}=6\cdot 5=30$.
    </Typography>
  </AccordionDetails>
</Accordion>
  </TabItem>

<TabItem value="Пример №3" label="Пример №3">
   Вычислить $\left(\cfrac13 \right)^{\log_36}$
<Accordion>
  <AccordionSummary
    expandIcon={<ExpandMoreIcon />}
    aria-controls="panel1a-content"
    id="panel1a-header"
  >
    <Typography>
      <b>Решение</b>
    </Typography>
  </AccordionSummary>
  <AccordionDetails>
    <Typography>

Применим свойство степени и основное логарифмическое тождество:

   $\left(\cfrac13 \right)^{\log_36}=\left(3^{-1} \right)^{\log_36}=\left(3^{\log_36} \right)^{-1}=\cfrac16$
    </Typography>
  </AccordionDetails>
</Accordion>
  </TabItem>

</Tabs>


### Основные свойства логарифма

Для всех приведенных ниже свойств предполагается, что $x>0, y>0, a>0, a \neq 1, m>0, m \neq 1$.


<br/>

:::tip Свойства логарифма

 01. $$ \log_{a}xy=\log_{a}x+\log_{a}y$$  (*логарифм произведения равен сумме логарифмов*).
02. $$ \log_{a}\cfrac{x}{y}=\log_{a}x-\log_{a}y$$  (*логарифм частного равен разности логарифмов*).
03. $$\log_ax^k=k\log_ax$$  (*вынесение показателя степени из аргумента логарифма*).
04. $$\log_ab=\cfrac{\log_mb}{\log_ma}$$ (*переход от одного основания к другому*).
05. $$\log_ab=\cfrac{1}{\log_ba}$$. 
06. $$\log_{a^{n}}b=\cfrac{1}{n}\log_ab$$.

:::


<Accordion>
  <AccordionSummary
    expandIcon={<ExpandMoreIcon />}
    aria-controls="panel1a-content"
    id="panel1a-header"
  >
    <Typography>
      <b>Доказательство свойства #1</b>
    </Typography>
  </AccordionSummary>
  <AccordionDetails>
    <Typography>

Рассмотрим выражения $a^{\log_axy}$ и $a^{\log_ax+\log_ay}$. Докажем, что они равны.

Используя основное логарифмическое тождество и свойства степени, запишем:
$$
\begin{gather*}
a^{\log_axy}=xy; \\
a^{\log_ax+\log_ay}=a^{\log_ax} \cdot a^{\log_ay}=xy. 
\end{gather*}
$$
Следовательно, $a^{\log_axy} =a^{\log_ax+\log_ay}$. Поскольку степени с одинаковым основанием равны, равны и их показатели. Таким образом, получаем: $\log_{a}xy=\log_{a}x+\log_{a}y$.

     </Typography>
  </AccordionDetails>
</Accordion>


<br />

<Accordion>
  <AccordionSummary
    expandIcon={<ExpandMoreIcon />}
    aria-controls="panel1a-content"
    id="panel1a-header"
  >
    <Typography>
      <b>Доказательство свойства #3</b>
    </Typography>
  </AccordionSummary>
  <AccordionDetails>
    <Typography>

Рассмотрим выражения: $a^{\log_ax^k}$ и $a^{k\log_ax}$. Докажем, что они равны.

Имеем: $a^{\log_ax^k}=x^k;$

$a^{k\log_ax}=\left(a^{\log_ax} \right)^k=x^k$.

Следовательно, $a^{\log_ax^k}=a^{k\log_ax}$. Поскольку степени с одинаковым основанием равны, равны и их показатели. 

Таким образом, получаем: $\log_ax^k=k\log_ax$.

     </Typography>
  </AccordionDetails>
</Accordion>

<br/>




---

<br/>







<Tabs>
  <TabItem value="Пример 1" label="Пример №1" default>
 Вычислить $\log_63+\log_62$.

 <Accordion>
  <AccordionSummary
    expandIcon={<ExpandMoreIcon />}
    aria-controls="panel1a-content"
    id="panel1a-header"
  >
    <Typography>
      <b>Решение</b>
    </Typography>
  </AccordionSummary>
  <AccordionDetails>
    <Typography>

$\log_63+\log_62=\log_66=1$


    </Typography>
  </AccordionDetails>
</Accordion>

  </TabItem>
<TabItem value="Пример №2" label="Пример №2">
   Вычислить $\cfrac{\log_5{64}}{\log_54}$.
<Accordion>
  <AccordionSummary
    expandIcon={<ExpandMoreIcon />}
    aria-controls="panel1a-content"
    id="panel1a-header"
  >
    <Typography>
      <b>Решение</b>
    </Typography>
  </AccordionSummary>
  <AccordionDetails>
    <Typography>

$\cfrac{\log_5{64}}{\log_54}=\log_464=3$
    </Typography>
  </AccordionDetails>
</Accordion>
  </TabItem>

<TabItem value="Пример №3" label="Пример №3">
   Вычислить $2\lg 5 + \cfrac12 \lg 16$.
<Accordion>
  <AccordionSummary
    expandIcon={<ExpandMoreIcon />}
    aria-controls="panel1a-content"
    id="panel1a-header"
  >
    <Typography>
      <b>Решение</b>
    </Typography>
  </AccordionSummary>
  <AccordionDetails>
    <Typography>

$2\lg 5 + \cfrac12 \lg 16 = \lg 25 + \lg 4 = \lg 100 =2$
    </Typography>
  </AccordionDetails>
</Accordion>
  </TabItem>

</Tabs>

---

### Идеи и алгоритмы решения задач

Успешное вычисление логарифмических выражений основывается на:

- 📚 прочном знании свойств логарифмов;
- 🔧 владении общими приемами их преобразования, которые укрепляются через регулярную практическую работу.

Основные логарифмические формулы удобно разделить на две группы: _формулы упаковки_ и _формулы распаковки_. Эти термины наглядно отражают процесс преобразования логарифмических выражений.


| **Формулы упаковки**<br />              |**Формулы распаковки**                 |
|:---------------------------------------:|:-------------------------------------:|
| $\log_ax+\log_ay=\log_axy$              | $\log_axy=\log_ax+\log_ay$           |    
| $\log_{a}x-\log_{a}y=\log_{a}\cfrac{x}{y}$|$\log_{a}\cfrac{x}{y}=\log_{a}x-\log_{a}y $ |  
| $k\log_ax=\log_ax^k$                      |     $\log_ax^k=k\log_ax$        |              
|$\cfrac{1}{n}\log_ab=\log_{a^{n}}b$      |$\log_{a^{n}}b=\cfrac{1}{n}\log_ab$ |


#### 💡Идея #1

Сразу обратите внимание на логарифм $\log_ab$: не являются ли его основание $a$ и аргумент $b$ степенями одного и того же числа $c$? Если это так, то логарифм можно считать практически вычисленным.

Например, $\log_{\frac{\sqrt{3}}{2}}\cfrac{64}{27}= \log_{\frac{\sqrt{3}}{2}}\left(\cfrac{2}{\sqrt{3}} \right)^6=-6$.



